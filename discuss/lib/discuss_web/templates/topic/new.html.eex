<%= form_for @changeset, Routes.topic_path(@conn, :create), fn f -> %>
    <div class="form-group">
        <%= text_input f, :title, placeholder: "Title" %>
        <%= error_tag f, :title %>
    </div>

    <%= submit "Save Topic", class: "button" %>
<% end %>